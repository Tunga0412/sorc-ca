# Validated in the unattended updater environment on 2026-08-26 with Python 3.13.7.
# Update deliberately, then run the full smoke and release checks.
flask==3.1.3
pandas==3.0.3
beautifulsoup4==4.14.3
requests==2.34.2
lxml==6.1.1

